{
  "name": "user_authentication",
  "description": "deployment",
  "node-main": "./server/run.js",
  "scripts": {
    "preinstall": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm run product",
    "heroku-postbuild": "echo This runs afterwards."
  },
  "engines": {
    "node": "7.1.0",
    "npm": "3.10.9"
  },
  "author": "will.chen",
  "license": "ISC"
}
