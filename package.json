{
  "name": "user_authentication",
  "description": "deployment",
  "node-main": "./server/run.js",
  "scripts": {
    "heroku-prebuild": "echo This runs before heroku installs your dependencies",
    "start": "cd client && npm install && npm run build && cd ../server && npm install && npm run product",
    "heroku-postbuild": "echo This runs afterwards."
  },
  "engines": {
    "node": "7.0.0",
    "npm": "3.10.9"
  },
  "author": "will.chen",
  "license": "ISC"
}
